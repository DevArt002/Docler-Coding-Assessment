{
  "name": "docler_coding_assessment",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "install-all": "cd client && yarn && cd .. && cd server && yarn",
    "dev-server": "cd server && yarn dev",
    "dev-client": "cd client && yarn dev",
    "dev": "concurrently \"yarn dev-client\" \"yarn dev-server\""
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
